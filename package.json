{
  "name": "viscum",
  "version": "0.0.1",
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^20.0.0",
    "oxlint": "^0.15.0",
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "^5.0.0"
  },
  "description": "Multi-agent AI system with Turborepo",
  "engines": {
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.1.0",
  "private": true,
  "scripts": {
    "s": "turbo run dev",
    "b": "turbo run build",
    "p": "./scripts/performance.sh",
    "l": "turbo run lint",
    "f": "turbo run format",
    "clean": "turbo run clean",
    "server": "cd server && bun run dev",
    "web": "cd web && bun run dev",
    "cli": "cd cli && bun run dev"
  },
  "type": "module",
  "workspaces": [
    "server",
    "web",
    "cli"
  ]
}
